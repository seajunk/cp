#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

/*
   first recall that for numbers a and b, there exists coeffiients x and y such that ax + by = gcd(a, b) (bezout identity)

   the extended euclidean algorithm calcualtes gcd(a, b) along with x and y which satisfies the bezout identity 



*/

void solve(){

}

int main(){
    ll t; cin >> t;
    while(t--) solve();
}
