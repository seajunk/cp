#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main(){
    ll n; cin >> n;
    ll k[n];
    ll sum = 0;
    for(int i = 0; i < n; i++){
        cin >> k[i];
        sum += k[i];
    }

    ll capacity = sum / 2;

    ll mx = 0;








}
