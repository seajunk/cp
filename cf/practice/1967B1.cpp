#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(){
    ll n, m; cin >> n >> m;

    // count # of (a, b) st
    // a in [1, n]
    // b in [1, m]
    // a + b is multiple of b*gcd(a, b)







}

int main(){
    ll t; cin >> t;
    while(t--) solve();
}
