#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(){
    ll n; cin >> n;
    set<ll> a;
    for(int i = 0; i < n; i++){
        ll in; cin >> in;
        a.insert(in);
    }

    // can have permutation of 





}

int main(){
    ll t; cin >> t;
    while(t--) solve();
}
