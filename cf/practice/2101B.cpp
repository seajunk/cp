#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

/*
   clearly parity of elements doesnt change after operation

   "We can see that the parity of the number of inversions is fixed"
   what the fuck ?

   Proof.





*/

ll cnt_inversions(vector<ll> arr){
    return -1;
}

void solve(){
    ll n; cin >> n;
    ll a[n];
    for(int i = 0; i < n; i++) cin >> a[i];






}

int main(){
    ll t; cin >> t;
    while(t--) solve();
}
