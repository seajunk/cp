#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(){
    ll n; cin >> n;
    ll a[n];
    for(int i = 0; i < n; i++) cin >> a[i];

    // conj: subarray b suitable => the unique subseqeunce is b itself
    // conj: any non contiguous subsequence cant be suitable

    // 1 2 1 2 -> (1 2 1 2) (1 2 1) NOT (1) (1 2) 







}

int main(){
    ll t; cin >> t;
    while(t--) solve();
}
