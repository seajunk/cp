#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(){
    ll n, k; cin >> n >> k;
    ll a[n], b[n];
    for(int i = 0; i < n; i++) cin >> a[i];
    for(int i = 0; i < n; i++) cin >> b[i];

    /*
       profit = cnt * price
       cnt = # of (a, b) st price <= b 
       bad = # of (a, b) st a < price <= b






    */











}

int main(){
    ll t; cin >> t;
    while(t--) solve();
}
