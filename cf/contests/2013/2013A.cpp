#include <bits/stdc++.h>
#define ll long long
using namespace std;

void solve(){
    ll n, x, y; cin >> n >> x >> y;

    cout << ((n + min(x, y) - 1) / min(x, y)) << '\n';





}

int main(){
    ll t; cin >> t;
    while(t--) solve();
}
