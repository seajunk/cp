#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(){
    ll n, q; cin >> n >> q;
    string s; cin >> s;
    vector<vector<char>> queries(q, vector<char>(2));
    for(int i = 0; i < q; i++){
        cin >> queries[i][0];
        cin >> queries[i][1];
    }












}

int main(){
    ll t; cin >> t;
    while(t--) solve();
}
